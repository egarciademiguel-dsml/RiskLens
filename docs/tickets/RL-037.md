# RL-037 — Update Notebook Conclusions to Match Actual Results

## Status: Done

## Goal
Rewrite the conclusions section of `model_deep_dive.ipynb` so it reflects what the analysis actually found after RL-031..RL-035, instead of the pre-baked narrative that survived from RL-028. Also remove the duplicate conclusions cell.

## Scope
- Delete the older duplicate conclusions cell ("## 8. Conclusions") that survived a previous edit.
- Rewrite the surviving conclusions cell ("## 9. Conclusions") to incorporate:
  - RL-031 stability vs accuracy distinction
  - RL-032 diagnosis (per-regime alpha=0 root cause)
  - RL-033 / RL-034 the two-stage fix and post-fix breach numbers
  - RL-035 evidence-led failure-mode taxonomy (Baseline beats GARCH+t overall)
  - The headline result that the "more complex" model is not automatically more accurate
- Do not touch any code cell or any analytic.

## Files changed
- `notebooks/model_deep_dive.ipynb` (one cell deleted, one cell rewritten)

## Acceptance criteria
- Only one conclusions section remains.
- Conclusions reference the actual breach numbers from RL-035 (Baseline 3.8%, GARCH+t 8.3%, MS-GARCH+EVT 5.4%).
- Stability vs accuracy is named explicitly.
- The pre-baked claim that "MS-GARCH is the most realistic model" is replaced with the evidence-based claim that it is the *only* model that gets level structure right while keeping in-regime reactivity.

## Decisions
- Conclusions are evidence-led, not narrative-led. Every claim is traceable to a numbered section in the notebook or a report under `reports/`.
- The "Why the old approach failed" subsection is kept (it explains a real prior bug fixed in RL-026) but moved into a footnote-style block to avoid burying the new findings.
