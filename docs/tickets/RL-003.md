# RL-003 — Environment & Reproducibility

## ID

RL-003

## Title

Environment & Reproducibility

## Objective

Establish a predictable, reproducible project environment so any developer can clone, install, and run RiskLens from scratch.

## Includes

- Updated `requirements.txt` with all plausible project dependencies.
- Updated `.gitignore` to cover venv, caches, data directories, and OS files.
- Updated `README.md` with full setup instructions and project structure.
- Created `Makefile` for developer convenience.
- Created `data/interim/` directory.
- Updated `docs/workflow.md` with corrected ticket execution order.
- Updated `docs/project_overview.md` with environment section.
- Updated `docs/ticket_registry.md` with RL-003 and reordered entries.
- Updated `docs/prompts_registry.md` with full RL-003 prompt.

## Excludes

- Docker or containerization.
- pyproject.toml changes.
- Poetry or any alternative package manager.
- Business logic or feature code.

## Files Created / Modified

- `requirements.txt` (modified)
- `.gitignore` (modified)
- `README.md` (modified)
- `Makefile` (created)
- `data/interim/` (created)
- `docs/workflow.md` (modified)
- `docs/project_overview.md` (modified)
- `docs/ticket_registry.md` (modified)
- `docs/prompts_registry.md` (modified)
- `docs/tickets/RL-003.md` (created)

## Acceptance Criteria

- A fresh clone + venv + `pip install -r requirements.txt` works without errors.
- `python -c "import pandas; import numpy; import yfinance; print('OK')"` succeeds.
- `.gitignore` covers `.venv/`, `__pycache__/`, `*.pyc`, `.ipynb_checkpoints/`, `.DS_Store`, `.env`, `data/raw/`, `data/interim/`, `data/processed/`.
- `README.md` contains full setup instructions for Windows and Mac/Linux.
- Ticket registry reflects correct execution order: RL-001 → RL-003 → RL-002.
- Full prompt stored in `docs/prompts_registry.md`.

## Notes

- This ticket was logically reordered to execute before RL-002. Environment setup must precede feature implementation for reproducibility.
- Makefile added as a minimal convenience layer, not a build system.
